# RevitServer-MaxBytesPerRead-Fix
Interactive PowerShell tool for Autodesk Revit Server to manage maxBytesPerRead values across all installed Revit Server versions. Supports backup creation, service restart, restore to default values, logging, and EXE deployment.
